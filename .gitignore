# rebuilt on every run
data/processed/
output/

# Bloomberg-sourced data: licensed to the terminal user, not for redistribution.
# Re-create on a machine with a terminal via `python run.py --refresh`.
data/raw/bbg_vix_impvol.csv

__pycache__/
*.pyc
